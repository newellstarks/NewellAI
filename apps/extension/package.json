{
  "name": "@newellai/extension",
  "version": "0.0.0",
  "private": true,
  "description": "NewellAI Chrome extension — Capture Client v1 durable queue + sync",
  "scripts": {
    "build": "esbuild src/background.ts src/options.ts src/content/chatgpt.ts --bundle --format=esm --outdir=dist --sourcemap --entry-names=[name]",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@newellai/contracts": "0.0.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.25.0",
    "fake-indexeddb": "^6.0.0",
    "happy-dom": "^15.11.7",
    "typescript": "^5.8.2",
    "vitest": "^3.0.9"
  }
}
