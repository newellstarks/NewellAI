{
  "name": "@newellai/worker",
  "version": "0.0.0",
  "private": true,
  "description": "NewellAI Worker — authenticated ingest, validation, D1 persistence",
  "scripts": {
    "dev:fs-bridge": "node ./scripts/artifact-fs-bridge.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "echo \"worker build uses wrangler\"",
    "test": "vitest run"
  },
  "dependencies": {
    "@newellai/contracts": "0.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250317.0",
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^22.13.10",
    "better-sqlite3": "^13.0.2",
    "typescript": "^5.8.2",
    "vitest": "^3.0.9",
    "wrangler": "^4.0.0"
  }
}
