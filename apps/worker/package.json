{
  "name": "@newellai/worker",
  "version": "0.0.0",
  "private": true,
  "description": "NewellAI Worker — authenticated ingest, validation, D1 persistence",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "echo \"worker build uses wrangler\"",
    "test": "echo \"worker tests not implemented yet\""
  },
  "dependencies": {
    "@newellai/contracts": "0.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250317.0",
    "typescript": "^5.8.2",
    "wrangler": "^4.0.0"
  }
}
