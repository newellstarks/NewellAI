# NewellAI Worker — ingest skeleton (accept → validate → respond).
# D1 binding remains commented until migrations exist; wire then.
name = "newellai-worker"
main = "src/index.ts"
compatibility_date = "2025-03-01"

# [[d1_databases]]
# binding = "DB"
# database_name = "newellai"
# database_id = "REPLACE_ME"
